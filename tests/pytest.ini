[pytest]
markers =
    hardware: tests requiring physical USB device
    dfu: DFU/MCUmgr tests
    slow: tests that take >10 seconds (uploads, reboots)
addopts = --strict-markers -v
log_cli = true
log_cli_level = INFO
timeout = 60
